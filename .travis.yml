language: cpp

compiler: gcc

matrix:
  include:
    - os: windows


before_install:
- echo $LANG
- echo $LC_ALL


install:
  - PowerShell -Command 'Set-ExecutionPolicy -ExecutionPolicy RemoteSigned'

script:
    - PowerShell -File run.ps1