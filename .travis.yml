language: cpp

compiler: gcc

env:
    global:
        - CODECOV_TOKEN="8ad626de-f68d-4889-aa49-dae87caed5d4"

matrix:
  include:
    - os: linux


before_install:
    - echo "Before install"
    - export CODECOV_TOKEN="token"
    #install dos2unix to convert endline character of my script file to a linux script file
    # - sudo apt-get install dos2unix



install:
 # - PowerShell -Command 'Set-ExecutionPolicy -ExecutionPolicy RemoteSigned'

script: cmake . -DCMAKE_CXX_FLAGS=-std=c++11 && make && ./testChange
    #- dos2unix runLinux.sh
    #- bash runLinux.sh
    #- PowerShell -File run.ps1     # This is not working   
    # - echo "token" > .cc_token
    # - curl -s https://codecov.io/bash -t @.cc_token